import { type ModelViewProps } from '@/utils/types'

export default function ModelView({
  index,
  groupRef,
  gsapType,
  controlRef,
  setRotationState,
  item,
  size
}: ModelViewProps) {
  return (
    <div>
      Enter
    </div >
  )
}
